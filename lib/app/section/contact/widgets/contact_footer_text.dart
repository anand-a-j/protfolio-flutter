import 'package:flutter/material.dart';

class ContactFooterText extends StatelessWidget {
  const ContactFooterText({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text(
      "Built in Flutter — Anand",
      style: TextStyle(
        color: Colors.white,
      ),
    );
  }
}
